# Roadmap Notes

- Improve real FTS/BM25 support in environments where DuckDB extensions are unavailable.
- Add optional multi-corpus support with corpus filtering in RAG tools.
- Consider a theme mode between light and dark after core public-release work is stable.
